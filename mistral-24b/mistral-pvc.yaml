apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mistral-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ssd
